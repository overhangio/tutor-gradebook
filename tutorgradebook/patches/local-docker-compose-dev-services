gradebook:
    image: {{ GRADEBOOK_DOCKER_IMAGE }}
    ports:
        - "127.0.0.1:1994:1994"
    # Unfortunately we can't rely on "npm run start" because we need to manually disable host checking,
    # which defaults to localhost/0.0.0.0.
    # command: ["npm", "run", "start"]
    command: ["webpack-dev-server", "--config=./node_modules/@edx/frontend-build/config/webpack.dev.config.js", "--progress", "--disable-host-check", "--output-public-path={{ GRADEBOOK_PUBLIC_PATH }}/", "--history-api-fallback={{ GRADEBOOK_PUBLIC_PATH }}/index.html"]
    environment:
        - NODE_ENV=development
    volumes:
        - ../plugins/mfe/apps/env/development.env:/openedx/env/development/00-mfe.env:ro
        - ../plugins/gradebook/apps/env/development.env:/openedx/env/development/01-gradebook.env:ro
